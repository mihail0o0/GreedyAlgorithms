function test(uloge: string[]): number {
    let index = -1;

    for (let i = 0; i < uloge.length; i++) {
         
    }

    return index;
}

const testCause = ['p', 'l', 'l', 'p', 'l'];
console.log(test(testCause));